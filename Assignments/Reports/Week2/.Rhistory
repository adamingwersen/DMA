len <- 21
fibo_val <- numeric(len)
fibo_val[1] <- 1
fibo_val[2] <- 1
for (i in 3:len) {
fibo_val[i] <- fibo_val[i-1] + fibo_val[i-2]
}
len <- 66
fibo_val <- numeric(len)
fibo_val[1] <- 1
fibo_val[2] <- 1
for (i in 3:len) {
fibo_val[i] <- fibo_val[i-1] + fibo_val[i-2]
}
len <- 666
fibo_val <- numeric(len)
fibo_val[1] <- 1
fibo_val[2] <- 1
for (i in 3:len) {
fibo_val[i] <- fibo_val[i-1] + fibo_val[i-2]
}
tail(fibo_val 5)
tail(fibo_val, 5)
for (i in length(fibo_val)) {
rest <- fibo_val[i + 1] % fibo_val[i]
cnt <- cnt + 1
}
